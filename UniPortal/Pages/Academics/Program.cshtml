@page
@model UniPortal.Pages.Academics.ProgramModel
@{
    ViewData["Title"] = "Program Management";
    Layout = "_AdminLayout";
}

<div class="container my-4">
    <h2 class="mb-4">🎓 Program Management</h2>

    <!-- Search Box -->
    <form method="get" class="mb-3">
        <div class="input-group">
            <input type="text" name="SearchTerm" value="@Model.SearchTerm" class="form-control" placeholder="Search by program name or code..." />
            <button type="submit" class="btn btn-primary">Search</button>
        </div>
    </form>

    <!-- Add New Program -->
    <form method="post">
        <table class="table table-striped">
            <thead class="table-dark">
                <tr>
                    <th>#</th>
                    <th>Code</th>
                    <th>Name</th>
                    <th>Department</th>
                    <th>Head</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>New</td>
                    <td><input asp-for="NewProgram.Code" class="form-control" required /></td>
                    <td><input asp-for="NewProgram.Name" class="form-control" required /></td>
                    <td>
                        <select asp-for="NewProgram.DepartmentId" class="form-control" required>
                            <option value="">-- Select Department --</option>
                            @foreach (var dept in Model.Departments)
                            {
                                <option value="@dept.Id">@dept.Name</option>
                            }
                        </select>
                    </td>
                    <td>
                        <button type="submit" asp-page-handler="Create" class="btn btn-success btn-sm">Add</button>
                    </td>
                </tr>
            </tbody>
        </table>
    </form>

    <!-- Existing Programs Table -->
    <table class="table table-striped">
        <thead class="table-dark">
            <tr>
                <th>Code</th>
                <th>Name</th>
                <th>Department</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var program in Model.Programs)
            {
                <tr>
                    <form method="post">
                        <input type="hidden" name="id" value="@program.Id" />

                        @if (Model.EditProgramId == program.Id.ToString())
                        {
                            <!-- Editing Row -->
                        <td><input name="EditProgram.Code" value="@Model.EditProgram.Code" class="form-control" /></td>
                        <td><input name="EditProgram.Name" value="@Model.EditProgram.Name" class="form-control" /></td>
                        <td>
                            <select name="EditProgram.DepartmentId" class="form-control">
                                @foreach (var dept in Model.Departments)
                                {
                                    <option value="@dept.Id" selected="@(Model.EditProgram.DepartmentId == dept.Id ? "selected" : null)">
                                        @dept.Name
                                    </option>
                                }
                            </select>
                        </td>
                        <td>
                            <button type="submit" asp-page-handler="SaveEdit" class="btn btn-sm btn-primary">💾 Save</button>
                            <button type="submit" asp-page-handler="CancelEdit" class="btn btn-sm btn-secondary">❌ Cancel</button>
                        </td>
                    }
                    else
                    {
                        <!-- Normal Row -->
                        <td>@program.Code</td>
                        <td>@program.Name</td>
                        <td>@program.Department.Name</td>
                        <td>
                            <button type="submit" asp-page-handler="Edit" name="id" value="@program.Id" class="btn btn-sm btn-warning">Edit</button>
                            @if (!program.IsDeleted)
                            {
                                <button type="button" class="btn btn-sm btn-danger" onclick="showDeleteModal('@program.Id', '@Url.Page("", "", new { handler = "Delete" }, null)')"> Delete </button>
                            }
                            else
                            {
                                <button type="submit" asp-page-handler="Activate" name="id" value="@program.Id" class="btn btn-sm btn-success">Activate</button>
                            }
                        </td>
                    }
                    </form>
                </tr>
            }
        </tbody>
    </table>

    <!-- Pagination -->
    <nav>
        <ul class="pagination">
            <li class="page-item @(Model.CurrentPage == 1 ? "disabled" : "")">
                <a class="page-link" href="?CurrentPage=@(Model.CurrentPage - 1)&SearchTerm=@Model.SearchTerm">Previous</a>
            </li>
            @for (int i = 1; i <= Model.TotalPages; i++)
            {
                <li class="page-item @(i == Model.CurrentPage ? "active" : "")">
                    <a class="page-link" href="?CurrentPage=@i&SearchTerm=@Model.SearchTerm">@i</a>
                </li>
            }
            <li class="page-item @(Model.CurrentPage == Model.TotalPages ? "disabled" : "")">
                <a class="page-link" href="?CurrentPage=@(Model.CurrentPage + 1)&SearchTerm=@Model.SearchTerm">Next</a>
            </li>
        </ul>
    </nav>
</div>
