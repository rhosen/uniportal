@page
@model UniPortal.Pages.Account.LoginModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    ViewData["Title"] = "Login";
}

<div class="container-fluid mt-lg-5 d-flex align-items-center justify-content-center bg-light mb-lg-5">
    <div class="col-12 col-sm-10 col-md-10 col-lg-8 col-xl-6">
        <div class="row shadow rounded-4 overflow-hidden bg-white">

            <!-- Left Panel: Illustration & Welcome (hidden on mobile) -->
            <div class="col-md-6 d-none d-md-flex bg-secondary align-items-center justify-content-center">
                <div class="text-white text-center px-4">
                    <img src="~/img/undraw_login_weas.svg" alt="Login Illustration" class="img-fluid mb-4" style="max-height: 260px;" />
                    <h2 class="fw-bold">Welcome Back</h2>
                    <p class="small">Sign in to access UniPortal</p>
                </div>
            </div>

            <!-- Right Panel: Login Form -->
            <div class="col-12 col-md-6 p-4 p-md-5 d-flex align-items-center">
                <div class="w-100">
                    <div class="text-center mb-4">
                        <h3 class="fw-bold text-primary">UniPortal</h3>
                        <p class="text-muted small">Secure Login</p>
                    </div>

                    @if (!ViewData.ModelState.IsValid)
                    {
                        <div class="alert alert-danger alert-dismissible fade show" role="alert">
                            @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                            {
                                <div>@error.ErrorMessage</div>
                            }
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    }

                    @if (TempData["Message"] != null)
                    {
                        <div class="alert alert-success alert-dismissible fade show" role="alert">
                            @TempData["Message"]
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    }

                    <form method="post">
                        <div class="mb-3">
                            <label asp-for="Input.Email" class="form-label fw-semibold"></label>
                            <input asp-for="Input.Email" class="form-control form-control-sm rounded-3" placeholder="Enter your email" />
                            <span asp-validation-for="Input.Email" class="text-danger small"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Input.Password" class="form-label fw-semibold"></label>
                            <input asp-for="Input.Password" type="password" class="form-control form-control-sm rounded-3" placeholder="Enter your password" />
                            <span asp-validation-for="Input.Password" class="text-danger small"></span>
                        </div>

                        <div class="mb-3 form-check">
                            <input asp-for="Input.RememberMe" class="form-check-input" />
                            <label asp-for="Input.RememberMe" class="form-check-label small">Remember Me</label>
                        </div>

                        <button type="submit" class="btn btn-primary w-100 fw-semibold btn-sm">
                            Login
                        </button>

                        <div class="text-center mt-3 small">
                            Don't have an account?
                            <a asp-page="/account/register" class="text-decoration-none fw-semibold">Register</a>
                        </div>
                    </form>
                </div>
            </div>

        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
